<%= form_for @conn, file_upload_path(@conn, :upload), [as: "upload", multipart: :true], fn f -> %>
  <%= file_input f, :file, accept: "image/*" %>
  <button class="btn btn-primary" type="submit">Upload</button>
<% end %>
